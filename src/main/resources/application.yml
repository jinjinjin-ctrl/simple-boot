server:
  port: 8080
  servlet:
    context-path: /douyin

spring:
  profiles:
    active: local
  security:
    user:
      name: zdx
      password: 123456
      roles: admin
  thymeleaf:
    cache: false
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/simple?useUnicode=true&serverTimezone=Asia/Shanghai&characterEncoding=utf8&autoReconnect=true&useSSL=false&allowMultiQueries=true&allowPublicKeyRetrieval=true
    username: root
    password: a2018117031.
mybatis-plus:
  mapper-locations: classpath:mapper/*Mapper.xml
